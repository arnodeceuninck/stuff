[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "auth-service"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
    "passlib[bcrypt]>=1.7",
]
