[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "auth-service"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
    "bcrypt>=5.0.0",
]
